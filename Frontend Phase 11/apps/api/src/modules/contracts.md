# Phase 11 domain contracts

Core modules: auth, identity, organisations, requests, consent, documents, verification, proofs, permissions, notifications, webhooks, audit, developer.

Security invariants: opaque public IDs; least-privilege scopes; consent enforcement; hashed secrets/tokens; append-only audit concept; no raw document content in logs/webhooks.
