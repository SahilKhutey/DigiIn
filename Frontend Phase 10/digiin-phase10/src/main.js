const root=document.getElementById('app');
const esc=s=>String(s).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const nav=`<header><strong>DigiIn</strong><nav><a href="#/">Home</a><a href="#/notifications">Notifications</a><a href="#/status">Status</a><a href="#/organisation/developer">Developer</a></nav></header>`;
const card=(title,body)=>`<section class="card"><h2>${title}</h2>${body}</section>`;
function render(){const p=location.hash.slice(1)||'/'; let html=nav;
if(p==='/notifications'){const n=JSON.parse(localStorage.getItem('digiin.notifications')||'[]');html+=`<main><h1>Notifications</h1>${card('Notification centre',n.length?n.map(x=>`<article class="notice ${x.read?'read':'unread'}"><strong>${esc(x.title)}</strong><p>${esc(x.message)}</p><small>${new Date(x.createdAt).toLocaleString()}</small></article>`).join(''):'<p>No notifications.</p>')}</main>`}
else if(p==='/status'){html+=`<main><h1>DigiIn service status</h1>${card('✓ All systems operational',['Verification','DigiLocker integration','Proof verification','Organisation portal','API'].map(x=>`<div class="row"><span>${x}</span><b>Operational</b></div>`).join(''))}</main>`}
else if(p==='/organisation/developer'){html+=`<main><h1>Developer platform</h1>${card('API usage',`<div class="stats"><div><b>1,284</b><span>Requests</span></div><div><b>97.9%</b><span>Success</span></div><div><b>183 ms</b><span>Avg latency</span></div></div>`)}${card('Integrations',['DigiLocker','Verification Engine','Notification Service','Webhook Gateway'].map(x=>`<div class="row"><span>${x}</span><b>Operational</b></div>`).join(''))}${card('Webhook delivery',`<div class="row"><span>verification.completed</span><b>Delivered</b></div><div class="row"><span>proof.created</span><b>Delivered</b></div><div class="row"><span>verification.failed</span><b>Failed</b></div>`)}</main>`}
else {html+=`<main><h1>Phase 10 — Platform Integration</h1><p>Notifications, events, webhooks, analytics, health and integration foundations.</p>${card('Platform layers','<ul><li>Event-driven notification foundation</li><li>Webhook delivery lifecycle</li><li>API usage analytics and rate-limit model</li><li>Service health and public status</li><li>Integration registry</li><li>Environment and feature configuration</li></ul>')}</main>`}
root.innerHTML=html;}
window.addEventListener('hashchange',render);render();
