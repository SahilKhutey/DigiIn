"use client";
import{useEffect,useState}from'react';import{api}from'../../lib/api';
export default function Review(){const[docs,setDocs]=useState<any[]>([]),[msg,setMsg]=useState('');async function load(){try{setDocs(await api('/review/documents'))}catch(e:any){setMsg(e.message)}}useEffect(()=>{load()},[]);async function act(id:string,decision:string){try{await api(`/review/documents/${id}/decision`,{method:'POST',body:JSON.stringify({decision,reason:'Reviewer action'})});setMsg(`Document ${decision.toLowerCase()}`);load()}catch(e:any){setMsg(e.message)}}return <main className="shell"><div className="nav"><div className="brand">Government Review</div><a href="/">Citizen app</a></div><section className="card"><h1>Verification queue</h1><p>{msg}</p></section><section className="grid">{docs.map(d=><article className="card" key={d.id}><h3>{d.title}</h3><p>{d.document_type}</p><p>{d.verification_status}</p><div className="row"><button className="primary" onClick={()=>act(d.id,'APPROVE')}>Approve</button><button className="secondary" onClick={()=>act(d.id,'REQUEST_CORRECTION')}>Request correction</button><button className="secondary" onClick={()=>act(d.id,'REJECT')}>Reject</button></div></article>)}</section></main>}
