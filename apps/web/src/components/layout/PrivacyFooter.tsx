export function PrivacyFooter() {
  return (
    <section id="privacy" className="privacy" aria-labelledby="privacy-heading">
      <h2 id="privacy-heading">Your information stays yours.</h2>
      <p>
        Use official services for identity verification. This recovery and verification platform
        holds only privacy-minimised transaction evidence in a future authorised deployment.
      </p>
    </section>
  );
}
