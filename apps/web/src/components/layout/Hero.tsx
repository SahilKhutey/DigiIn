export function Hero() {
  return (
    <section className="hero">
      <p className="eyebrow">VERIFY ONCE. PROVE ANYWHERE.</p>
      <h1>Government portals should request verified claims, not copies of documents.</h1>
      <p>
        DigiIn demonstrates purpose-bound credential proofs, citizen consent, document recovery,
        immutable versioning, and privacy-minimised receipts with synthetic data.
      </p>
    </section>
  );
}
