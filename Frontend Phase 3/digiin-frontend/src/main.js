import {Button,Badge,Card,Alert,Field,Status,DocumentCard,Stepper} from './components.js';

const app=document.querySelector('#app');
const state={menu:false,lang:'EN',user:null,consent:false};
const path=()=>location.hash.replace(/^#/,'')||'/';
const go=p=>location.hash=p;

const Header=()=>`<div class="top"><div class="container top-inner"><span>Government service experience foundation</span><span>Accessible • Secure • Citizen-first</span></div></div><header class="header"><div class="container head"><a class="brand" href="#/" aria-label="DigiIn home"><span class="mark">DI</span><span>DigiIn<small>Digital document verification</small></span></a><nav class="nav" aria-label="Primary"><a href="#/about">About</a><a href="#/how">How it works</a><a href="#/citizens">For citizens</a><a href="#/organisations">For organisations</a><a href="#/security">Security</a><a href="#/help">Help</a></nav><div class="actions"><a class="text-link" href="#/accessibility">Accessibility</a><a class="btn btn-secondary btn-small" href="#/help">Help</a><a class="btn btn-primary btn-small" href="#/sign-in">Sign in</a><button class="menu" id="menu" type="button" aria-label="Open menu" aria-expanded="${state.menu}">☰</button></div></div>${state.menu?`<nav class="mobile-nav" aria-label="Mobile navigation"><a href="#/about">About DigiIn</a><a href="#/how">How it works</a><a href="#/citizens">For citizens</a><a href="#/organisations">For organisations</a><a href="#/security">Security & privacy</a><a href="#/accessibility">Accessibility</a><a href="#/help">Help & FAQ</a><a href="#/sign-in">Sign in</a></nav>`:''}</header>`;

const Footer=()=>`<footer class="footer"><div class="container footer-grid"><div><strong>DigiIn</strong><p class="muted">Verify once. Share securely.</p><p class="footer-note">A citizen-first foundation for trusted digital document verification.</p></div><div><strong>Service</strong><a href="#/about">About DigiIn</a><a href="#/how">How it works</a><a href="#/citizens">For citizens</a><a href="#/organisations">For organisations</a></div><div><strong>Trust & support</strong><a href="#/security">Security & privacy</a><a href="#/accessibility">Accessibility</a><a href="#/help">Help & FAQ</a><a href="#/contact">Contact</a></div></div><div class="container footer-bottom"><small>Prototype foundation • No real documents are processed in demo mode.</small><span><a href="#/privacy">Privacy</a> · <a href="#/terms">Terms</a></span></div></footer>`;

const Page=({eyebrow,title,description,children,narrow=false,actions=''})=>`<main id="main" class="page"><div class="container ${narrow?'narrow':''}">${eyebrow?`<span class="eyebrow">${eyebrow}</span>`:''}<h1>${title}</h1>${description?`<p class="lead muted">${description}</p>`:''}${actions?`<div class="page-actions">${actions}</div>`:''}${children}</div></main>`;
const Section=({eyebrow,title,text,children,className=''})=>`<section class="section ${className}"><div class="container"><div class="section-heading"><div>${eyebrow?`<span class="eyebrow">${eyebrow}</span>`:''}<h2>${title}</h2></div>${text?`<p class="muted">${text}</p>`:''}</div>${children||''}</div></section>`;
const CheckList=items=>`<ul class="list">${items.map(x=>`<li>${x}</li>`).join('')}</ul>`;

function Home(){return `<main id="main"><section class="hero"><div class="container hero-grid"><div><span class="eyebrow">Secure digital verification</span><h1>Verify once.<br><span>Share securely.</span></h1><p>DigiIn helps citizens use verified digital documents across government and trusted services without repeatedly submitting the same documents.</p><div class="actions hero-actions">${Button({label:'Start verification',href:'#/verify',icon:'→'})}${Button({label:'See how it works',href:'#/how',variant:'secondary'})}</div><div class="trust-row"><span>✓ Consent-led</span><span>✓ Accessible</span><span>✓ Privacy-first</span></div></div>${Card({className:'hero-card',children:`<div class="shield">✓</div><h2>Consent-led by design</h2><p class="muted">You stay in control of what is shared, with whom, and for what purpose.</p>${CheckList(['The requesting organisation is clearly identified.','Only requested documents are used for the stated purpose.','Every verification has a clear, understandable status.'])}`})}</div></section>${Section({eyebrow:'Why DigiIn',title:'Less paperwork. More trust.',text:'A reusable verification layer for citizen services.',children:`<div class="grid3">${[['01','Verify digitally','Use trusted digital documents instead of repeatedly uploading copies.'],['02','Give informed consent','See who is asking, what they need and why before you approve.'],['03','Share proof','Share a verification result instead of exposing unnecessary document copies.']].map(([n,t,d])=>Card({className:'feature-card',children:`<div class="icon">${n}</div><h3>${t}</h3><p class="muted">${d}</p>`})).join('')}</div>`})}${Section({eyebrow:'The journey',title:'How verification works',children:`<div class="journey-grid"><div>${Stepper({steps:['Request','Consent','Verify','Result'],current:3})}</div>${Card({className:'journey-card',children:`<span class="eyebrow">Citizen control</span><h3>Every important decision is visible</h3><p class="muted">DigiIn makes the requester, purpose, documents, consent and result understandable before the citizen proceeds.</p><a class="link-button" href="#/how">Explore the journey →</a>`})}</div>`})}${Section({eyebrow:'Built for public services',title:'One foundation, many services',text:'Designed to work across citizen-facing verification journeys.',children:`<div class="audience-grid"><a class="audience-card" href="#/citizens"><span class="audience-icon">C</span><div><h3>For citizens</h3><p class="muted">Manage verified documents and approve secure verification requests.</p></div><span aria-hidden="true">→</span></a><a class="audience-card" href="#/organisations"><span class="audience-icon">O</span><div><h3>For organisations</h3><p class="muted">Request only the evidence you need and receive a verifiable result.</p></div><span aria-hidden="true">→</span></a></div>`})}<section class="section section-alt"><div class="container">${Card({className:'banner',children:`<div><span class="eyebrow banner-eyebrow">Working prototype</span><h2>Try the complete verification journey.</h2><p>Experience the citizen flow from request review to verification result.</p></div>${Button({label:'Start verification',href:'#/verify',variant:'secondary',icon:'→'})}`})}</div></section></main>`}

function About(){return Page({eyebrow:'About DigiIn',title:'A trusted verification layer for citizen services',description:'DigiIn is designed around one simple idea: citizens should not have to repeatedly submit the same documents to every service that needs to verify them.',children:`${Section({eyebrow:'The problem',title:'Repeated verification creates friction',text:'Citizens often encounter the same document request across admissions, employment, benefits and other services.',children:`<div class="content-grid"><div>${Card({children:`<h3>Today</h3>${CheckList(['Upload or submit the same document repeatedly.','Share more information than the service actually needs.','Uncertainty about whether a document is genuinely verified.'])}`})}</div><div>${Card({children:`<h3>With DigiIn</h3>${CheckList(['Connect trusted digital documents once.','Give purpose-specific, informed consent.','Share a verification result with authorised services.'])}`})}</div></div>`})}${Section({eyebrow:'Our principles',title:'Designed for trust',children:`<div class="grid3">${[['Citizen control','Consent is explicit and understandable.'],['Least data','Share only what the verification requires.'],['Transparency','Make status, purpose and requester visible.']].map(([t,d])=>Card({children:`<h3>${t}</h3><p class="muted">${d}</p>`})).join('')}</div>`})}`})}

function How(){return Page({eyebrow:'How DigiIn works',title:'A clear journey from request to proof',description:'The experience is designed so a citizen can understand every important step.',children:`<div class="process-list">${[['01','Request','An organisation identifies itself and requests only the documents needed for a defined purpose.'],['02','Review & consent','The citizen sees the requester, purpose and requested documents before giving consent.'],['03','Verify','DigiIn retrieves the relevant trusted records and checks the requested information.'],['04','Result','The service receives a verification result and reference instead of unnecessary document copies.']].map(([n,t,d],i)=>Card({className:'process-card',children:`<div class="process-number">${n}</div><div><h2>${t}</h2><p class="muted">${d}</p>${i===1?'<span class="process-note">Consent is never hidden behind generic agreement language.</span>':''}</div>`})).join('')}</div>${Alert({title:'Prototype note',message:'The current hackathon build simulates the trusted-source connection and verification response. Real integrations will replace these demo states.',tone:'info'})}`})}

function Citizens(){return Page({eyebrow:'For citizens',title:'Your documents. Your consent. Your choice.',description:'DigiIn gives citizens a simpler way to respond when a trusted service needs to verify documents.',actions:Button({label:'Try verification',href:'#/verify',icon:'→'}),children:`<div class="feature-split">${Card({children:`<span class="eyebrow">Your DigiIn account</span><h2>Keep verification ready</h2><p class="muted">See your documents, verification history and active requests from one citizen dashboard.</p>${CheckList(['A clear DigiIn ID for authorised services.','Document verification status at a glance.','Reviewable requests and consent decisions.'])}`})}${Card({children:`<span class="eyebrow">When someone requests verification</span><h2>Know before you share</h2><p class="muted">You can see the organisation, purpose and documents requested before approving the request.</p>${CheckList(['No generic “I agree” consent.','Purpose-specific sharing.','Clear success and failure states.'])}`})}</div>${Section({eyebrow:'Citizen journey',title:'Simple enough to understand at a glance',children:`<div class="journey-cards">${['Review the request','Give consent','Verify documents','Share the result'].map((x,i)=>Card({children:`<div class="icon">0${i+1}</div><h3>${x}</h3>`})).join('')}</div>`})}`})}

function Organisations(){return Page({eyebrow:'For organisations',title:'Request the evidence you need — not everything else',description:'DigiIn can provide a consistent verification experience for departments, institutions and trusted service providers.',actions:Button({label:'Explore verification demo',href:'#/verify',icon:'→'}),children:`<div class="content-grid"><div>${Card({children:`<span class="eyebrow">Create a request</span><h2>Define the purpose</h2><p class="muted">Identify your organisation and request only the document types necessary for the service.</p>${CheckList(['Purpose-specific request.','Requested-document visibility.','Consent before verification.'])}`})}</div><div>${Card({children:`<span class="eyebrow">Receive a result</span><h2>Verify without storing unnecessary copies</h2><p class="muted">The organisation can receive a verification status and reference that can be audited or checked according to the service policy.</p>${CheckList(['Verified / pending / failed status.','Verification reference.','Clear failure and recovery states.'])}`})}</div></div>${Alert({title:'Integration-ready direction',message:'The frontend is structured to connect to request, consent, verification and audit APIs in the integration phase.',tone:'info'})}`})}

function Security(){return Page({eyebrow:'Security & privacy',title:'Security should be visible in the experience',description:'DigiIn treats privacy and security as user-facing design requirements, not only backend controls.',children:`<div class="security-grid">${[['Least data','The experience is designed around requesting and sharing only the information needed for the stated purpose.'],['Explicit consent','A citizen can see the requester, purpose and requested documents before approving.'],['Clear status','Verification states are understandable and do not depend on colour alone.'],['Traceable result','A verification reference gives the service and citizen a shared point of reference.']].map(([t,d])=>Card({children:`<h2>${t}</h2><p class="muted">${d}</p>`})).join('')}</div><div class="section-inline">${Alert({title:'Prototype limitation',message:'This frontend contains demonstration data only. Production security will require authenticated sessions, secure API transport, server-side authorisation, audit logging, consent records and real trusted-source integration.',tone:'warning'})}</div>`})}

function Accessibility(){return Page({eyebrow:'Accessibility',title:'Designed so more citizens can complete the journey',description:'DigiIn uses an accessibility-first frontend foundation aligned with the UX4G approach.',children:`<div class="grid3">${[['Keyboard','Core actions and navigation are available without a mouse.'],['Focus','Interactive controls use visible keyboard focus indicators.'],['Screen readers','Semantic landmarks, labels and live status regions support assistive technology.'],['Responsive','Layouts adapt from mobile to desktop without hiding essential actions.'],['Motion','Reduced-motion preferences are respected.'],['Content','Errors and verification states are communicated with text, not colour alone.']].map(([t,d])=>Card({children:`<h3>${t}</h3><p class="muted">${d}</p>`})).join('')}</div>${Alert({title:'Accessibility testing',message:'Before production release, the complete product should be tested with keyboard navigation, screen readers, contrast tooling and representative users with disabilities.',tone:'info'})}`})}

function Help(){const faqs=[['What is DigiIn?','DigiIn is a verification experience that helps citizens share trusted verification results with authorised services instead of repeatedly submitting document copies.'],['Does the prototype process real documents?','No. The hackathon frontend uses demonstration content and simulated verification states.'],['What happens when I give consent?','The production flow will record a purpose-specific consent decision and share only the information permitted by that request.'],['Can an organisation see all my documents?','The intended design is request-specific. An organisation should receive only the evidence needed for the authorised verification purpose.'],['What if verification fails?','The product will explain the failure in plain language and provide a recovery action where possible.']];return Page({eyebrow:'Help & FAQ',title:'Questions about DigiIn?',description:'Start with the answers below. The help centre will grow as integrations and service journeys are added.',children:`<div class="faq-list">${faqs.map(([q,a],i)=>`<details class="faq" ${i===0?'open':''}><summary>${q}<span aria-hidden="true">+</span></summary><p class="muted">${a}</p></details>`).join('')}</div><div class="support-card">${Card({children:`<span class="eyebrow">Still need help?</span><h2>Contact the DigiIn team</h2><p class="muted">For the prototype, use the contact form to record a support question.</p><a class="btn btn-primary" href="#/contact">Contact us</a>`})}</div>`})}

function Contact(){return Page({eyebrow:'Contact',title:'Tell us how we can help',description:'This prototype form records a demonstration message only.',narrow:true,children:`<form id="contact-form" class="form-card">${Field({id:'name',label:'Name',placeholder:'Your name',required:true})}${Field({id:'email',label:'Email',type:'email',placeholder:'name@example.gov.in',required:true})}<div class="field"><label for="topic">Topic</label><select id="topic"><option>General question</option><option>Citizen support</option><option>Organisation integration</option><option>Accessibility</option></select></div><div class="field"><label for="message">Message</label><textarea id="message" rows="5" placeholder="How can we help?"></textarea></div>${Button({label:'Send message',type:'submit',className:'block'})}<div id="contact-status" aria-live="polite"></div></form>`})}

function SignIn(){return Page({eyebrow:'Citizen account',title:'Sign in to DigiIn',description:'Use your registered mobile number to continue.',narrow:true,children:`${Alert({title:'Demo mode',message:'Any valid 10-digit mobile number is accepted. No real identity data is processed.',tone:'info'})}<form id="signin" class="form-card">${Field({id:'mobile',label:'Mobile number',hint:'Enter the 10-digit number registered with your account.',type:'tel',placeholder:'10-digit mobile number',required:true})}${Button({label:'Continue',type:'submit',className:'block'})}<p class="form-footer">By continuing, you agree to the DigiIn <a href="#/terms">terms</a> and <a href="#/privacy">privacy notice</a>.</p></form>`})}

function Dashboard(){return Page({eyebrow:'Citizen account',title:'Welcome back',description:'Manage your verified documents and active requests.',children:`<div class="actions"><a class="btn btn-primary" href="#/verify">New verification <span aria-hidden="true">→</span></a></div><div class="stats"><div class="stat-card"><strong>12</strong><span>Documents</span></div><div class="stat-card"><strong>9</strong><span>Verified</span></div><div class="stat-card"><strong>2</strong><span>Pending</span></div></div><div class="dashboard-grid">${Card({children:`<div class="card-heading"><div><span class="eyebrow">Your identity</span><h2>DigiIn ID</h2></div>${Badge({label:'Active',tone:'success',icon:'✓'})}</div><div class="id-card"><small>DigiIn ID</small><strong>DIN-84K2-19Q7</strong><p>Use this ID with authorised services to start a verification request.</p><button class="btn btn-secondary btn-small" id="copy-id" type="button">Copy ID</button></div>`})}${Card({children:`<div class="card-heading"><div><span class="eyebrow">Activity</span><h2>Recent activity</h2></div></div><ul class="activity"><li><span class="activity-dot success"></span><div><strong>Class 12 verified</strong><small>Today • CBSE</small></div></li><li><span class="activity-dot info"></span><div><strong>University request received</strong><small>Yesterday</small></div></li></ul>`})}</div><section class="documents-section"><div class="section-heading"><div><span class="eyebrow">Document vault</span><h2>Your documents</h2></div><button class="btn btn-secondary" type="button">Filter</button></div><div class="document-grid">${DocumentCard({title:'Class 12 Certificate',issuer:'CBSE',detail:'Issued 15 May 2025',status:'verified'})}${DocumentCard({title:'Class 10 Certificate',issuer:'CBSE',detail:'Issued 20 May 2023',status:'verified'})}${DocumentCard({title:'Identity document',issuer:'Connected source',detail:'Awaiting verification',status:'pending'})}</div></section>`})}

function Verify(){return Page({eyebrow:'Step 1 of 4 • Review request',title:'Verify your documents',description:'ABC University is requesting verification for an admission application.',narrow:true,children:`${Stepper({steps:['Request','Consent','Verify','Result'],current:0})}<div class="form-card" id="verify-card"><div class="card-heading"><div><h2>What will be shared?</h2><p class="muted">Only the documents listed in this request.</p></div>${Badge({label:'2 documents',tone:'info'})}</div><div class="requester"><div class="org-mark">AU</div><div><strong>ABC University</strong><span>Admission verification</span></div></div><div class="requested-list"><div class="requested"><div><strong>Class 10 Certificate</strong><small>Eligibility verification</small></div>${Status({status:'verified'})}</div><div class="requested"><div><strong>Class 12 Certificate</strong><small>Eligibility verification</small></div>${Status({status:'verified'})}</div></div>${Alert({title:'You stay in control.',message:'DigiIn will share only the requested verification result for this purpose. You can review the request before consenting.',tone:'info'})}<label class="check-field"><input id="consent" type="checkbox"><span>I understand and consent to verification for this purpose.</span></label><button id="verify" class="btn btn-primary block" type="button" disabled>Continue with consent <span aria-hidden="true">→</span></button><div id="status" aria-live="polite" class="status-region"></div></div>`})}

function Result(){return Page({eyebrow:'Step 4 of 4 • Verification result',title:'Verification complete',description:'2 of 2 requested documents were successfully verified.',narrow:true,children:`<div class="result-card"><div class="result-icon">✓</div>${Badge({label:'Verified',tone:'success',icon:'✓'})}<div class="result-summary"><strong>2 of 2</strong><span>documents verified</span></div><div class="verification-list"><div>${Status({status:'verified'})}<span>Class 10 Certificate</span><small>CBSE</small></div><div>${Status({status:'verified'})}<span>Class 12 Certificate</span><small>CBSE</small></div></div><div class="verification-id"><small>Verification ID</small><strong>DIN-VRF-82A91</strong><button id="share" class="btn btn-secondary btn-small" type="button">Copy ID</button></div><p id="sharemsg" class="muted" aria-live="polite"></p><div class="actions centered"><a class="btn btn-primary" href="#/dashboard">Go to dashboard</a><a class="btn btn-secondary" href="#/verify">Start another</a></div></div>`})}

const Info=({title,description,heading,body})=>Page({eyebrow:'Information',title,description,narrow:true,children:Card({children:`<h2>${heading}</h2><p class="muted">${body}</p>`})});

function render(){
  const p=path();
  let c;
  switch(p){
    case '/': c=Home(); break;
    case '/about': c=About(); break;
    case '/how': c=How(); break;
    case '/citizens': c=Citizens(); break;
    case '/organisations': c=Organisations(); break;
    case '/security': c=Security(); break;
    case '/accessibility': c=Accessibility(); break;
    case '/help': c=Help(); break;
    case '/contact': c=Contact(); break;
    case '/sign-in': c=SignIn(); break;
    case '/dashboard': c=Dashboard(); break;
    case '/verify': c=Verify(); break;
    case '/result': c=Result(); break;
    case '/privacy': c=Info({title:'Privacy notice',description:'A clear explanation of how DigiIn is intended to handle information.',heading:'Privacy by purpose',body:'Production privacy notices will define the exact information collected, legal basis, retention, sharing, consent records and user rights. This prototype contains demonstration data only.'}); break;
    case '/terms': c=Info({title:'Terms of service',description:'The service conditions for the DigiIn foundation experience.',heading:'Prototype terms',body:'This hackathon build is a demonstration. Production terms, service responsibilities and organisation eligibility requirements will be defined before deployment.'}); break;
    default: c=Page({eyebrow:'404',title:'Page not found',description:'The page you requested does not exist.',children:Card({children:`<p class="muted">Return to the DigiIn home page or use the navigation above.</p><a class="btn btn-primary" href="#/">Go home</a>`})});
  }
  app.innerHTML='<a class="skip" href="#main">Skip to main content</a>'+Header()+c+Footer();
  bind();
}

function bind(){
  document.querySelector('#menu')?.addEventListener('click',()=>{state.menu=!state.menu;render()});
  document.querySelectorAll('.mobile-nav a').forEach(a=>a.addEventListener('click',()=>{state.menu=false}));
  document.querySelector('#signin')?.addEventListener('submit',e=>{e.preventDefault();const mobile=document.querySelector('#mobile').value;if(!/^\d{10}$/.test(mobile)){document.querySelector('#mobile').focus();return}state.user={mobile};go('/dashboard')});
  document.querySelector('#copy-id')?.addEventListener('click',async e=>{try{await navigator.clipboard.writeText('DIN-84K2-19Q7');e.currentTarget.textContent='Copied ✓'}catch{e.currentTarget.textContent='DIN-84K2-19Q7'}});
  const consent=document.querySelector('#consent'),verify=document.querySelector('#verify'),status=document.querySelector('#status');
  consent?.addEventListener('change',()=>{state.consent=consent.checked;verify.disabled=!consent.checked});
  verify?.addEventListener('click',()=>{verify.disabled=true;verify.innerHTML='Connecting to trusted source…';status.innerHTML=Alert({message:'Connecting to trusted source…',tone:'info'});setTimeout(()=>{verify.innerHTML='Verifying documents…';status.innerHTML=Alert({message:'Documents retrieved. Checking issuing sources…',tone:'info'});setTimeout(()=>go('/result'),1200)},900)});
  document.querySelector('#share')?.addEventListener('click',async e=>{const m=document.querySelector('#sharemsg');try{await navigator.clipboard.writeText('DigiIn Verification ID: DIN-VRF-82A91');e.currentTarget.textContent='Copied ✓';m.textContent='Verification ID copied. Share it only with the authorised organisation.'}catch{m.textContent='Verification ID: DIN-VRF-82A91'}});
  document.querySelector('#contact-form')?.addEventListener('submit',e=>{e.preventDefault();document.querySelector('#contact-status').innerHTML=Alert({title:'Message recorded',message:'Thanks. This prototype does not send email; the message was accepted as a demo interaction.',tone:'success'})});
}
window.addEventListener('hashchange',()=>{state.menu=false;render()});
render();
