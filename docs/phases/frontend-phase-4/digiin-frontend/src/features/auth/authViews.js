import {Button,Card,Alert,Field} from '../../components.js';

export const OtpInput=()=>`<div class="otp-grid" role="group" aria-label="6-digit verification code">${Array.from({length:6},(_,i)=>`<input class="otp-input" id="otp-${i}" inputmode="numeric" autocomplete="one-time-code" maxlength="1" aria-label="Digit ${i+1} of 6">`).join('')}</div>`;

export const Onboarding=({profile=null})=>`<main id="main" class="page"><div class="container narrow"><span class="eyebrow">First-time setup</span><h1>Welcome to DigiIn</h1><p class="lead muted">Create your citizen profile so you can manage verification requests and your DigiIn ID.</p><form id="onboarding-form" class="form-card">${Alert({title:'Your account is yours.',message:'DigiIn uses your profile to personalise the service. Your DigiIn ID identifies your account; it does not reveal your documents by itself.',tone:'info'})}${Field({id:'full-name',label:'Full name',hint:'Use the name you want displayed in your DigiIn account.',placeholder:'Enter your full name',required:true,value:profile?.name||''})}<fieldset class="language-field"><legend>Preferred language</legend><label><input type="radio" name="language" value="en" checked> English</label><label><input type="radio" name="language" value="hi"> हिन्दी</label></fieldset><label class="check-field"><input id="account-consent" type="checkbox"><span>I understand that DigiIn will create a citizen account for me and keep the information needed to operate the service.</span></label><div id="onboarding-error" aria-live="polite"></div>${Button({label:'Create my DigiIn account',type:'submit',className:'block'})}</form></div></main>`;
